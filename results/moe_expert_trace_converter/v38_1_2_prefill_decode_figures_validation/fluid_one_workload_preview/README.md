# DO NOT USE AS A CONSISTENT RESULT

This one-workload preview is deprecated. It mixed values from different fluid conventions:

- some bars came from strict dispatch+combine sequential fluid values;
- some bars came from old V38 max(dispatch, combine) fluid values;
- fair-universal selection also came from a different workload pool.

Therefore the numbers intentionally do **not** match the later three-workload figure. The later three-workload figure is internally consistent, but it uses a different convention.

Use this directory only as historical scratch output, not as a supervisor or paper figure.
